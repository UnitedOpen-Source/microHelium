server {
    listen 80;
    listen [::]:80;
    server_name localhost;

    root /var/www/html/public;
    index index.php index.html index.htm;

    # Issue #90: X-Frame-Options, X-Content-Type-Options, Referrer-Policy,
    # Content-Security-Policy, Permissions-Policy and the Cross-Origin-*
    # headers are set by App\Http\Middleware\SecurityHeaders instead, so
    # they hold for every way this app is served -- `artisan serve`, another
    # reverse proxy, a different container -- and not only for this file.
    # Repeating them here would send each one twice.

    charset utf-8;

    # Security headers
    add_header X-XSS-Protection "1; mode=block" always;

    # Logging
    access_log /var/log/nginx/microhelium_access.log;
    error_log /var/log/nginx/microhelium_error.log;

    # Laravel routing
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM configuration
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;

        # Timeouts
        fastcgi_connect_timeout 300;
        fastcgi_send_timeout 300;
        fastcgi_read_timeout 300;
        fastcgi_buffer_size 64k;
        fastcgi_buffers 4 64k;
        fastcgi_busy_buffers_size 128k;
        fastcgi_temp_file_write_size 256k;
    }

    # Deny access to .htaccess files
    location ~ /\.ht {
        deny all;
    }

    # Deny access to .env files
    location ~ /\.env {
        deny all;
    }

    # Deny access to .git
    location ~ /\.git {
        deny all;
    }

    # Static file caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|pdf|txt|woff|woff2|ttf|eot|svg)$ {
        expires 1y;
        # nosniff has to be repeated per location: nginx drops inherited
        # add_header directives as soon as a location declares one of its
        # own, and these responses never reach PHP, so SecurityHeaders does
        # not see them. A ZAP scan caught /img/favicon.ico going out bare.
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options "nosniff" always;
        add_header Cross-Origin-Resource-Policy "same-origin" always;
        access_log off;
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Vite HMR (development only)
    # Uncomment when running with: docker compose --profile dev up
    # location /vite-hmr {
    #     proxy_pass http://node:5173;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    #     proxy_set_header Host $host;
    # }

    # Robots.txt
    location = /robots.txt {
        allow all;
        log_not_found off;
        access_log off;
        add_header X-Content-Type-Options "nosniff" always;
        # Issue #143: the favicon location below sets this one and this
        # location did not, for no reason anyone recorded. Same argument as
        # there -- nginx drops inherited add_header the moment a location
        # declares one of its own, so every static location repeats them.
        add_header Cross-Origin-Resource-Policy "same-origin" always;
    }

    # Favicon
    location = /favicon.ico {
        log_not_found off;
        access_log off;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Cross-Origin-Resource-Policy "same-origin" always;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
        add_header X-Content-Type-Options "nosniff" always;
    }

    # PHP-FPM status (restricted)
    location ~ ^/(fpm-status|fpm-ping)$ {
        allow 127.0.0.1;
        allow 172.16.0.0/12;
        deny all;
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }
}

# HTTPS server (optional, uncomment if using SSL)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name localhost;
#
#     root /var/www/html/public;
#     index index.php;
#
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#
#     # ... rest of configuration same as HTTP
# }
